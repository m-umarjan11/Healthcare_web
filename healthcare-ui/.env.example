# Base URL of the Flask API (flask_api.py). Defaults to http://127.0.0.1:5000
# for local development if unset. In Vercel, set this as a project environment
# variable pointing at your deployed backend, then redeploy.
VITE_API_BASE_URL=http://127.0.0.1:5000
